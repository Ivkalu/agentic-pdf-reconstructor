services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    env_file:
      - .env
    environment:
      - WORKSPACE_PATH=/workspace
      - PORT=3000
